library identifier: "JNFILES@main", retriever: modernSCM(
  [$class: 'GitSCMSource',
   remote: 'https://github.com/Flame0n/JNFILES.git',
   credentialsId: 'AVK'])

def options = [repositoryUrl: "https://github.com/ROCmSoftwarePlatform/tensorflow-upstream.git/",
              repositoryBranch: "r2.9-rocm-enhanced"]

nightly(options)